import { h, FunctionComponent } from 'preact';
import { Stage } from '../../containers/Stage';

type StagesProps = {};

export const StagesComponent: FunctionComponent<StagesProps> = ({}) => (
  <Stage />
);
